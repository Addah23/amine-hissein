Branch: feat/carousel

What I added in this branch:

- index_carousel.html: A standalone version of your portfolio page that uses a JavaScript carousel and references the existing image files at the repository root (Image Portefolio 1..4.jpeg). This avoids modifying the large embedded-data index.html and keeps the original intact while providing the requested carousel UX.

- portfolio_amine_carousel.html: Same carousel page under a different name (copy) so you can compare and choose which file to keep or to merge into index.html.

Next steps I suggest:
1) Preview the new pages by opening these URLs on GitHub Pages (if your repo uses Pages) or by pulling the branch and opening the files locally.
2) If you approve, I can replace index.html with the carousel version (index_carousel.html) or integrate the carousel directly into index.html. Note: index.html contains embedded base64 images which make direct edits riskier; replacing the file with a cleaned version is safer.

Files created in this commit:
- index_carousel.html
- portfolio_amine_carousel.html

If you want me to proceed and replace the main index.html file, reply "Replace index.html" and I'll update index.html on the feat/carousel branch accordingly.
